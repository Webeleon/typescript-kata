/**
 * Write a function using a regex to check if the base string include the query
 * @param {string} base
 * @param {string} query
 * @returns {boolean}
 */
export const include = (base: string, query: string): boolean => undefined;
