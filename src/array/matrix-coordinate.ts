/**
 * return the number at the specified coordinates or undefined
 * @param {number} x
 * @param {number} y
 * @param {number[][]} matrix
 * @returns {number}
 */
export const matrixCoordinate = (
  x: number,
  y: number,
  matrix: number[][]
): number | undefined => {
  return 42;
};
